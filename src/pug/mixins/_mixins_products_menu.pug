mixin products_menu(prefix, product_Menu)
    each item, index in product_Menu
        +e.A.product-wrap.sec4(href="#")
            +e.IMG.img(src=item.productImgSrc)
            +e.SPAN.title #{item.productTitle}